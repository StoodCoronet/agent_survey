# Agent Survey 项目重构方案

## 设计原则

1. **轻量化优先**：不引入 Pydantic ORM、不抽象基类、不引入 DAG 编排。SQLite 保持 dict，只在需要处加类型提示。
2. **可读性优先**：目录编号体现执行顺序，CLI 保持语义化名字。
3. **业务驱动**：重构服务当前三步 pipeline，不超前设计未来 stage。
4. **零残留**：旧 `pipeline/` 在 commit 后直接删除，不做 shim 兼容。

---

## 新目录结构

```
src/agent_survey/
├── __init__.py
├── cli.py                    # 纯命令入口
├── tui.py                    # Rich 文本菜单引导（新增）
│
├── core/                     # 基础设施（稳定层）
│   ├── config.py             # 配置加载
│   ├── db.py                 # SQLite ORM（保持 dict）
│   └── console.py            # Rich console 单例
│
├── services/                 # 外部服务
│   ├── arxiv.py              # arXiv API
│   ├── s2.py                 # Semantic Scholar
│   ├── openreview.py         # OpenReview 新接口
│   ├── dblp.py               # DBLP 抓取
│   └── external.py           # COLM 等特殊源
│
├── stages/                   # 流水线（编号 = 执行顺序）
│   ├── s00_harvest.py
│   ├── s01_enrich.py         # 合并 enrich + enrich_arxiv，三级 fallback
│   ├── s02_prefilter.py
│   ├── s03_classify.py
│   ├── s04_fulltext.py
│   ├── s05_deepdive.py
│   └── s06_report.py         # 合并 markdown + obsidian
│
├── analysis/                 # 分析工具
│   ├── keyword_stats.py
│   └── estimate_cost.py
│
└── report/
    ├── markdown.py
    └── obsidian.py
```

---

## CLI & TUI 引导

### 现有 CLI 命令名保持不变
- `agent-survey harvest`
- `agent-survey enrich`
- `agent-survey classify`
- ...

### 新增 `agent-survey tui`（Rich 菜单）

采用**方案 C：状态面板 + 实时摘要**。主界面分上下两部分，上方是 pipeline 步骤，下方是实时数据面板 + 智能推荐。

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🤖 Agent Survey Pipeline                          ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫
┃                                                    ┃
┃  [✅] 1. harvest     ──────── 73,703 papers       ┃
┃  [❌] 2. enrich      ──────── 需先完成 harvest    ┃
┃  [✅] 3. prefilter   ──────── 1,320 hits          ┃
┃  [✅] 4. classify    ──────── core:635 related:3,249 ┃
┃  [○ ] 5. fulltext    ──────── 未开始              ┃
┃  [○ ] 6. deepdive    ──────── 未开始              ┃
┃  [○ ] 7. report      ──────── 未开始              ┃
┃                                                    ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫
┃  📊 当前数据库概况                                 ┃
┃  总论文: 73,703  |  有摘要: 8,421 (11.4%)         ┃
┃  core: 635  |  related: 3,249  |  adjacent: 7,576 ┃
┃                                                    ┃
┃  💡 推荐下一步: enrich (补齐 abstract)             ┃
┃  输入步骤号，或按 r 直接执行推荐:                  ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

**状态图标：**
- ✅ 绿色 = 已完成（有数据写入）
- ❌ 红色 = 缺失前置依赖（不可直接执行）
- ○ 灰色 = 未开始（无依赖问题）
- 🔄 黄色 = 运行中（按 Ctrl+C 中断后回到此菜单）

**快捷命令：**
- `1-7` 选择步骤执行
- `r` 执行推荐下一步
- `q` 退出
- `?` 查看详细统计（弹出 stats 表格后按任意键返回）

**依赖检查逻辑：**
- `classify` 依赖 `prefilter` 或 `enrich`（任一完成即可）。
- `enrich` 依赖 `harvest`。
- 如果用户选择有缺失依赖的步骤，TUI 提示："需要先跑 [X]，是否自动执行？"
- 非强制阻塞，用户可选择跳过依赖直接执行。

---

## Step 3: s01_enrich.py（Abstract 补齐）

### 策略
- **目标范围**：core + related + adjacent（总计 11,460 篇，优先）
- **是否给 irrelevant 补**：默认跳过，加 `--all` 参数可覆盖
- **来源优先级**：
  1. **arXiv**（免费，title 搜索匹配）→ 3 async workers，3s 间隔
  2. **Semantic Scholar**（免费 API，100 RPM）→ 5 workers
  3. **OpenReview**（ICLR/ICML/NeurIPS 论坛页面有 abstract）→ 5 workers
- **Fallback 规则**：
  - 前一源查到但 abstract 为空 → 视为未命中，继续下一源
  - 只有拿到非空 abstract 才停
- **Checkpoint**：每处理 100 篇 commit 一次，中断可续跑

### DB 字段新增
```sql
ALTER TABLE papers ADD COLUMN enrich_source TEXT;  -- 'arxiv' | 's2' | 'openreview' | NULL
ALTER TABLE papers ADD COLUMN enrich_at TEXT;       -- ISO timestamp
```

### Stats 输出（JSON + Terminal Panel）
```json
{
  "stage": "enrich",
  "timestamp": "20260514T120000Z",
  "processed": 11460,
  "filled": 5230,
  "failed": 120,
  "by_source": {"arxiv": 3100, "s2": 1800, "openreview": 330},
  "by_venue": {"ICLR": 450, "ICML": 320, "ICSE": 280, ...},
  "success_rate_pct": 45.6,
  "abstract_coverage_before": 12.3,
  "abstract_coverage_after": 58.1
}
```

---

## 迁移步骤

1. **Git commit 当前状态**
2. **创建新目录**：`core/`, `services/`, `stages/`, `analysis/`, `report/`
3. **迁移文件**（保持 git mv 记录历史）：
   - `config.py` → `core/config.py`
   - `db.py` → `core/db.py`
   - `console.py` → `core/console.py`
   - `llm/client.py` + `llm/prompts.py` → `services/llm.py`（合并为一个文件）
   - `sources/arxiv.py` → `services/arxiv.py`
   - `sources/semantic_scholar.py` → `services/s2.py`
   - `sources/dblp.py` → `services/dblp.py`
   - `sources/external.py` → `services/external.py`
   - `pipeline/harvest.py` → `stages/s00_harvest.py`
   - `pipeline/enrich.py` + `pipeline/enrich_arxiv.py` → `stages/s01_enrich.py`
   - ...（以此类推）
4. **更新所有 import 路径**
5. **删除旧目录**：`pipeline/`, `llm/`, `sources/`, `pdf/`（内容迁移到 services/ 或 stages/）
6. **新增 `tui.py`**
7. **DB migration**：新增 `enrich_source` 和 `enrich_at` 字段
8. **验证**：跑 `agent-survey classify --help` 确保无 import error

---

## 不做的决定

- ❌ 不引入 Pydantic ORM 或 SQLModel
- ❌ 不引入 StageRunner 基类或抽象层
- ❌ 不引入 DAG / pipeline 编排框架
- ❌ CLI 命令名不改编号（保持 `enrich` 而非 `s01-enrich`）
- ❌ 不做前后端分离或 Web UI
- ❌ 不做 pytest 框架（保持轻量）
